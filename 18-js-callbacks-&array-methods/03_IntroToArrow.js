const add = function(x,y){
    return x + y;
}

// or

// just a compact way, just replace function to arrow and move it in front of argument
const combine = (x,y) => {
    return x + y;
}

combine(10,10);


